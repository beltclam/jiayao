# jiayao

A custom animated Codex pixel pet.

The pet package and preview are proposed through a pull request. After merging, copy `pet.json` and `spritesheet.webp` into your Codex custom pets directory.
